apiVersion: v1
kind: Pod
metadata:
  name: gpu-test-2
spec:
  restartPolicy: Never
  containers:
  - name: cuda-container
    image: nvidia/cuda:12.9.0-base-ubuntu24.04
    command: ["bash", "-c", "nvidia-smi && sleep infinity"]
    resources:
      limits:
        nvidia.com/gpu: 1
